# Рантайм GZP. Список намеренно короткий: чем меньше зависимостей,
# тем меньше вес инсталлятора и меньше поводов для сбоя PyInstaller.

pillow>=10.0          # рендер загрузочного экрана и иконки

# Только Windows: доступ к котировкам напрямую из терминала.
MetaTrader5>=5.0.45; sys_platform == "win32"
